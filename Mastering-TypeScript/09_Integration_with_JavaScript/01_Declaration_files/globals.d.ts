/**
 * The `declare` keyword tells the compiler that we want to
 * define something, but that the implementation of this
 * object (or variable or function) will be resolved at runtime.
 */

declare const CONTACT_EMAIL_ARRAY: string[];
