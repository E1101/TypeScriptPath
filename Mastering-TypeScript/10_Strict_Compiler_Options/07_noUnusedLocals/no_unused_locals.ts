
function testFunctionErroneous(input: string): boolean {
    // let test;
    // return false;
}

function testFunctionFixed(input: string): boolean {
    let test = input;
    return test === "someValue";
}
